
<div class="row">
  <div class ="col-sm-8 offset-sm-2">
  <% @restaurants.each do |restaurant| %>
    <article class="mb-3">
      <h2><%= link_to restaurant.name, restaurant_path(restaurant), class: "text-decoration-none" %></h2>
      <p class="text-truncate"><%= restaurant.address %></p>
    </article>
  <% end %>
  </div>
</div>
